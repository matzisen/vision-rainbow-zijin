闲趣 (Xianqu) - 二手闲置交易平台闲趣 
是一款基于 Go (Gin) 和 Vue 3 开发的现代化二手闲置物品交易平台。项目采用前后端分离架构，不仅具备完整的商品发布、浏览、购买流程，还集成了 WebSocket 实时聊天功能和功能完善的后台管理系统。UI 采用清新的“奶黄色”风格，旨在为用户提供舒适、便捷的校园或社区交易体验。✨ 功能特性 (Features)🛍️ 用户端 (Client)用户体系：支持用户注册、登录 (JWT 鉴权)、个人信息修改、密码修改。商品浏览：首页瀑布流展示、商品分类筛选、关键词搜索。商品发布：支持图片上传 (拖拽/点击)、商品详情编辑、价格设定。交易流程：购物车管理 (添加/删除/查看)。完整订单流程：立即购买 -> 订单确认 -> 模拟支付 -> 订单状态流转 (待支付/已完成)。防超卖机制：基于数据库事务保证库存扣减的原子性。互动社交：实时聊天：基于 WebSocket 实现买卖双方即时通讯，支持查看历史消息。收藏夹：一键收藏心仪商品。🛡️ 管理端 (Admin)数据看板：系统核心数据统计与可视化。用户管理：查看用户列表，支持封禁/解封用户状态。商品审核：对发布的商品进行审核管理。订单管理：查看全平台订单状态与详情。🛠️ 技术栈 (Tech Stack)后端 (Backend)核心框架: Gin - 高性能 Web 框架ORM: GORM - 强大的 ORM 库数据库: SQLite (通过 glebarez/sqlite 驱动，轻量级无需配置 MySQL，可平滑切换)实时通讯: Gorilla WebSocket鉴权: JWT (JSON Web Tokens)配置管理: Go standard library & Viper (可选)前端 (Frontend)核心框架: Vue 3 (Composition API)构建工具: Vite 7UI 组件库: Element Plus状态管理: Pinia路由管理: Vue Router 4CSS 预处理: Sass网络请求: Axios (封装拦截器)📂 项目结构 (Directory Structure)Plaintextvision-rainbow-zijin/
├── backend/                # 后端项目 (Go)
│   ├── config/             # 数据库初始化与配置
│   ├── dist/               # 前端构建产物 (用于后端静态托管)
│   ├── internal/
│   │   ├── controllers/    # 控制器 (处理 HTTP 请求)
│   │   ├── middleware/     # 中间件 (CORS, JWT, AdminAuth)
│   │   ├── models/         # GORM 数据库模型
│   │   ├── services/       # 业务逻辑层
│   │   └── utils/          # 工具函数 (加密, JWT)
│   ├── pkg/
│   │   └── ws/             # WebSocket 核心逻辑 (Client, Hub)
│   ├── uploads/            # 图片上传存储目录
│   ├── main.go             # 程序入口 & 路由注册
│   └── go.mod              # Go 依赖管理
│
└── frontend/               # 前端项目 (Vue 3)
    ├── src/
    │   ├── assets/         # 静态资源 (CSS, Images)
    │   ├── components/     # 公共组件
    │   ├── stores/         # Pinia 状态管理
    │   ├── utils/          # 工具库 (request.js)
    │   ├── views/          # 页面视图 (Home, Cart, Admin, etc.)
    │   ├── App.vue         # 根组件
    │   └── main.js         # 入口文件
    └── package.json        # npm 依赖管理
🚀 快速开始 (Getting Started)1. 后端启动 (Backend)确保您的环境已安装 Go 1.24+。Bashcd backend

# 下载依赖
go mod tidy

# 启动服务
# 服务将运行在 http://localhost:8081
# 首次运行会自动创建 SQLite 数据库文件和 uploads 目录
go run main.go
2. 前端启动 (Frontend)确保您的环境已安装 Node.js (推荐 v20+)。Bashcd frontend

# 安装依赖
npm install

# 启动开发服务器
npm run dev
3. 生产环境部署 (Production)项目支持将前端打包后由 Go 后端直接托管，实现单文件部署。编译前端:Bashcd frontend
npm run build
# 构建产物将生成在 backend/dist/ 目录下 (需确保 vite.config.js 配置正确)
运行后端:Bashcd backend
go run main.go
# 访问 http://localhost:8081 即可使用完整系统
📸 预览 (Screenshots)(此处可以放置您项目 img.png 或其他截图的链接)首页 (Home)商品详情 (Detail)待补充聊天室 (Chat)后台管理 (Admin)待补充待补充📝 接口文档 (API Reference)后端 API 路由规范如下 (部分示例):Auth: POST /api/login, POST /api/registerProducts: GET /api/products, POST /api/products (需鉴权)Orders: POST /api/orders, POST /api/orders/:id/payChat: WS /api/ws (WebSocket 连接)Admin: POST /api/admin/login, GET /api/admin/stats
